# SA1019 (deprecated API) disabled because GKE and Pub/Sub APIs expose deprecated fields
# that are still needed for data parity with legacy infrastructure.
# ST1000-ST1022 (style checks) disabled to match golangci-lint configuration.
checks = ["all", "-SA1019", "-ST1000", "-ST1020", "-ST1021", "-ST1022"]
