sourceId: mandiant_advantage
secretKeys:
  - MANDIANT_ADVANTAGE_BASE_URL
  - MANDIANT_ADVANTAGE_TOKEN
runtimes:
  - localId: assets
    config:
      base_url: env:MANDIANT_ADVANTAGE_BASE_URL
      family: assets
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /v1/me
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:MANDIANT_ADVANTAGE_TOKEN
