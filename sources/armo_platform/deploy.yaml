sourceId: armo_platform
secretKeys:
  - ARMO_PLATFORM_BASE_URL
  - ARMO_PLATFORM_TOKEN
runtimes:
  - localId: assets
    config:
      base_url: env:ARMO_PLATFORM_BASE_URL
      family: assets
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /v1/me
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:ARMO_PLATFORM_TOKEN
