sourceId: probely
secretKeys:
  - PROBELY_TOKEN
runtimes:
  - localId: needs-attention-top
    config:
      family: needs_attention_top
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /targets/all/needs_attention_top/
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:PROBELY_TOKEN
