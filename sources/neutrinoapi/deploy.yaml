sourceId: neutrinoapi
secretKeys:
  - NEUTRINOAPI_API_TOKEN
runtimes:
  - localId: ip-blocklist
    config:
      family: ip_blocklist
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /ip-blocklist
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      api_token: env:NEUTRINOAPI_API_TOKEN
