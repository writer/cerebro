sourceId: okta
secretKeys:
  - OKTA_API_TOKEN
  - OKTA_DOMAIN
runtimes:
  - localId: audit
    config:
      domain: env:OKTA_DOMAIN
      family: audit
      per_page: "200"
      since: "2026-05-01T00:00:00Z"
      token: env:OKTA_API_TOKEN
  - localId: user
    config:
      domain: env:OKTA_DOMAIN
      family: user
      per_page: "200"
      token: env:OKTA_API_TOKEN
  - localId: group
    config:
      domain: env:OKTA_DOMAIN
      family: group
      per_page: "200"
      token: env:OKTA_API_TOKEN
  - localId: application
    config:
      domain: env:OKTA_DOMAIN
      family: application
      per_page: "200"
      token: env:OKTA_API_TOKEN
