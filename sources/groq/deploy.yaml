sourceId: groq
secretKeys:
  - GROQ_TOKEN
runtimes:
  - localId: model-catalog
    config:
      family: model_catalog
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /models
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:GROQ_TOKEN
