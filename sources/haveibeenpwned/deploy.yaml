sourceId: haveibeenpwned
secretKeys:
  - HAVEIBEENPWNED_API_TOKEN
runtimes:
  - localId: breaches
    config:
      family: breaches
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /breaches
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      api_token: env:HAVEIBEENPWNED_API_TOKEN
