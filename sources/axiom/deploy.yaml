sourceId: axiom
secretKeys:
  - AXIOM_BASE_URL
  - AXIOM_TOKEN
runtimes:
  - localId: alerts
    config:
      base_url: env:AXIOM_BASE_URL
      family: alerts
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /v1/me
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:AXIOM_TOKEN
