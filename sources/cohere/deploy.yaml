sourceId: cohere
secretKeys:
  - COHERE_TOKEN
runtimes:
  - localId: model-catalog
    config:
      family: model_catalog
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /v1/models
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:COHERE_TOKEN
  - localId: datasets
    config:
      family: datasets
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /v1/models
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:COHERE_TOKEN
  - localId: fine-tuned-models
    config:
      family: fine_tuned_models
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /v1/models
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:COHERE_TOKEN
  - localId: connectors
    config:
      family: connectors
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /v1/models
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:COHERE_TOKEN
