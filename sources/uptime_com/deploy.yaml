sourceId: uptime_com
secretKeys:
  - UPTIME_COM_BASE_URL
  - UPTIME_COM_TOKEN
runtimes:
  - localId: alerts
    config:
      base_url: env:UPTIME_COM_BASE_URL
      family: alerts
      failure_modes: api_error,auth_error,rate_limit,schema_drift
      health_path: /v1/me
      expected_cadence_seconds: "86400"
      stale_after_seconds: "86400"
      per_page: "100"
      token: env:UPTIME_COM_TOKEN
