id: secret
class: secret
graph_label: Secret
urn_kind_prefix: "runtime_"
event_kind_prefix: true
description: Secret, key, or credential managed by a vault or secrets manager.
required_attributes:
  - tenant_id
  - source_event_id
  - secret_id
  - secret_name
