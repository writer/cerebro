entries:
  - classifier_output: supported
    definition:
      auth:
        credential_fields:
          - key: token
            reference_only: true
            secret: true
        model: bearer_token
      categories:
        - grc
        - compliance
      description: Collects users, controls, and findings from Drata and projects them into the Cerebro graph as users, assets, and findings for owner, account, evidence, and compliance context.
      display_name: Drata
      id: builtin-drata
      resource_families:
        - coverage:
            - control_domains:
                - asset_inventory
              evidence_types:
                - source_snapshot
              families:
                - users
              high_value: true
              id: users
              support: partial
              title: Users
              type: entity_family
          event:
            kind: drata.users
            required_payload_fields:
              - id
            schema_ref: drata/users/v1
            urn_kind: drata_users
          id: users
          id_field: id
          label: Users
          method: GET
          pagination:
            cursor_json_path: $.next_cursor
            cursor_param: cursor
            page_size: 100
            page_size_param: limit
            type: cursor
          path: /v1/users
          projection:
            fields:
              email: email|primary_email|profile.email
              status: status|state|lifecycle_state
              user_id: id
            template: identity_user
          record_selector: $.data[*]
        - coverage:
            - control_domains:
                - asset_inventory
              evidence_types:
                - source_snapshot
              families:
                - controls
              high_value: true
              id: controls
              support: partial
              title: Controls
              type: entity_family
          event:
            kind: drata.controls
            required_payload_fields:
              - id
            schema_ref: drata/controls/v1
            urn_kind: drata_controls
          id: controls
          id_field: id
          label: Controls
          method: GET
          pagination:
            cursor_json_path: $.next_cursor
            cursor_param: cursor
            page_size: 100
            page_size_param: limit
            type: cursor
          path: /v1/controls
          projection:
            fields:
              resource_id: id
              resource_type: resource_type|type|kind
              resource_urn: resource_urn|urn|metadata.resource_urn
            template: asset
          record_selector: $.data[*]
        - coverage:
            - control_domains:
                - remediation
              evidence_types:
                - remediation_state
              families:
                - findings
              high_value: true
              id: findings
              support: partial
              title: Findings
              type: remediation_state
          event:
            kind: drata.findings
            required_payload_fields:
              - id
            schema_ref: drata/findings/v1
            urn_kind: drata_findings
          id: findings
          id_field: id
          label: Findings
          method: GET
          pagination:
            cursor_json_path: $.next_cursor
            cursor_param: cursor
            page_size: 100
            page_size_param: limit
            type: cursor
          path: /v1/findings
          projection:
            fields:
              description: description|summary
              finding_id: id
              severity: severity|risk|priority
              status: status|state
            template: finding
          record_selector: $.data[*]
      schema_version: cerebro.integration/v1
      source_id: drata
      tenant_id: builtin_catalog
      transport:
        base_url: https://public-api.drata.com
        verification:
          expect_status:
            - 200
          method: GET
          path: /public/users
