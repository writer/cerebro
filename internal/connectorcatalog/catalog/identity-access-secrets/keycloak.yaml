entries:
- classifier_output: supported
  definition:
    schema_version: cerebro.integration/v1
    id: builtin-keycloak
    tenant_id: builtin_catalog
    source_id: keycloak
    display_name: Keycloak
    description: "Collects users, groups, and audit events from Keycloak and projects them into the Cerebro graph as users, groups, and audit events for identity, access, device, and secret context."
    categories:
    - identity
    - access
    auth:
      model: bearer_token
      credential_fields:
      - key: token
        secret: true
        reference_only: true
    transport:
      base_url: https://${config.realm_url}
      verification:
        method: GET
        path: "/users"
        expect_status:
        - 200
    resource_families:
    - id: users
      label: Users
      path: "/users"
      method: GET
      record_selector: "$.data[*]"
      id_field: id
      event:
        kind: keycloak.users
        schema_ref: keycloak/users/v1
      projection:
        template: identity_user
      coverage:
      - id: users
        type: entity_family
        title: Users
        families:
        - users
        support: partial
        high_value: true
        evidence_types:
        - source_snapshot
        control_domains:
        - asset_inventory
      pagination:
        type: cursor
        cursor_param: cursor
        cursor_json_path: "$.next_cursor"
        page_size_param: limit
        page_size: 100
    - id: groups
      label: Groups
      path: "/groups"
      method: GET
      record_selector: "$.data[*]"
      id_field: id
      event:
        kind: keycloak.groups
        schema_ref: keycloak/groups/v1
      projection:
        template: identity_group
      coverage:
      - id: groups
        type: entity_family
        title: Groups
        families:
        - groups
        support: partial
        high_value: true
        evidence_types:
        - source_snapshot
        control_domains:
        - asset_inventory
      pagination:
        type: cursor
        cursor_param: cursor
        cursor_json_path: "$.next_cursor"
        page_size_param: limit
        page_size: 100
    - id: audit_events
      label: Audit events
      path: "/events"
      method: GET
      record_selector: "$.data[*]"
      id_field: id
      event:
        kind: keycloak.audit_events
        schema_ref: keycloak/audit_events/v1
      projection:
        template: audit_event
      coverage:
      - id: audit_events
        type: audit_event
        title: Audit events
        families:
        - audit_events
        support: partial
        high_value: true
        evidence_types:
        - logging_configuration
        control_domains:
        - logging_monitoring
      pagination:
        type: cursor
        cursor_param: cursor
        cursor_json_path: "$.next_cursor"
        page_size_param: limit
        page_size: 100
