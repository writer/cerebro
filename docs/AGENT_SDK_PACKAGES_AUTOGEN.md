# Agent SDK Package Generation

Agent SDK package generation is retired on current `main`.

The TypeScript and Python helper clients are maintained as small bootstrap SDKs. They should only reference routes that exist in `api/openapi.yaml`.
