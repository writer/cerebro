# Agent SDK Contracts

The generated Agent SDK contract catalog is retired on current `main`.

The repository no longer contains the Agent SDK gateway handlers or generation scripts that previously produced this file. Supported SDK helpers are maintained directly under `sdk/python` and `sdk/typescript` and are limited to routes present in the current bootstrap OpenAPI surface.
