[project]
name = "cerebro-infra"
version = "0.1.0"
description = "Cerebro infrastructure with Pulumi"
requires-python = ">=3.11"
dependencies = [
    "pulumi>=3.100.0",
    "pulumi-aws>=6.20.0",
    "pulumi-random>=4.16.0",
    "pulumi-tailscale>=0.17.0",
]
