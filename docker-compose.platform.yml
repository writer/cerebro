version: "3.9"

# Kept for callers that still reference docker-compose.platform.yml. The
# bootstrap platform stack now uses the same NATS/Postgres/Neo4j dependencies as
# docker-compose.yml; Ensemble-era ClickHouse/Redis services are intentionally
# not part of the Cerebro local runtime.
include:
  - docker-compose.yml
