apiVersion: cerebro.writer.com/v1alpha1
kind: PolicyFindingRuleTest
cases:
    - name: matching resource produces a finding
      resource:
        spec:
            uses_latest_image_tag: true
      wantFinding: true
    - name: non-matching resource passes
      resource:
        spec:
            uses_latest_image_tag: false
      wantFinding: false
