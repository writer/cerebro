apiVersion: cerebro.writer.com/v1alpha1
kind: PolicyFindingRule
metadata:
    id: identity-forgerock-personal-email-domain
    name: Forgerock Accounts Using Personal Email Domains
    description: Detects active forgerock accounts using personal email domains instead of corporate identity domains.
    tags:
        - forgerock
        - identity
        - account-hygiene
        - governance
spec:
    severity: MEDIUM
    category: identity
    resourceType: forgerock_user
    match:
        query: SELECT u.id AS id, u.email AS email FROM forgerock_users u WHERE COALESCE(u.active, true) = true AND LOWER(COALESCE(u.email, '')) SIMILAR TO '%@(gmail|yahoo|hotmail|outlook)\\.com'
    remediation:
        summary: Migrate account identities to corporate domains and disable personal-domain access where not explicitly approved.
    frameworks:
        - name: SOC 2
          controls:
            - CC6.1
        - name: ISO 27001:2022
          controls:
            - A.5.17
    enabled: true
