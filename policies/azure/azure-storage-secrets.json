{
  "id": "azure-storage-secrets",
  "name": "Azure Storage Contains Potential Credentials",
  "description": "Azure Storage blob objects contain potential secrets, API keys, or credentials",
  "effect": "forbid",
  "resource": "azure::storage::blob",
  "conditions": [
    "name MATCHES '.*\\.(pem|key|pfx|credentials|env|secret)$' OR name MATCHES '.*(password|secret|connection.?string|api.?key).*'"
  ],
  "severity": "high",
  "remediation": "Move secrets to Azure Key Vault. Remove credential files from blob storage. Enable soft delete and versioning.",
  "tags": ["azure", "storage", "secrets", "data-exposure"]
}
