{
  "id": "container-resource-limits",
  "name": "Container Resource Limits Set",
  "description": "Ensures containers have CPU and memory limits defined",
  "severity": "MEDIUM",
  "category": "container",
  "resource_type": "container",
  "frameworks": [
    {
      "name": "SOC 2",
      "controls": [
        "CC1.1"
      ]
    },
    {
      "name": "CIS Benchmarks",
      "controls": [
        "1.1"
      ]
    }
  ],
  "enabled": true,
  "query": "SELECT id, name, image, namespace, cpu_limit, memory_limit FROM containers WHERE cpu_limit IS NULL OR memory_limit IS NULL",
  "remediation": "Define resource limits for all containers to prevent resource exhaustion and noisy neighbor issues.",
  "tags": [
    "container",
    "resources",
    "limits",
    "kubernetes"
  ]
}
