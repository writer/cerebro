{
  "id": "aws-elb-https-only",
  "name": "Load Balancer Listener Without HTTPS",
  "description": "Application or Network Load Balancer has a listener accepting unencrypted HTTP traffic",
  "effect": "forbid",
  "resource": "aws::elbv2::listener",
  "conditions": [
    "protocol == 'HTTP'",
    "default_actions NOT CONTAINS (type == 'redirect' AND redirect_config.protocol == 'HTTPS')"
  ],
  "severity": "medium",
  "remediation": "Configure HTTP listeners to redirect to HTTPS. Remove plain HTTP listeners if not needed for redirects.",
  "tags": [
    "aws",
    "elb",
    "encryption",
    "transport-security"
  ],
  "risk_categories": [
    "UNPROTECTED_DATA"
  ],
  "frameworks": [
    {
      "name": "NIST 800-53 r5",
      "controls": [
        "SC-12",
        "SC-13",
        "SC-28"
      ]
    },
    {
      "name": "PCI DSS v4.0.1",
      "controls": [
        "3.5",
        "4.2"
      ]
    },
    {
      "name": "CIS Controls v8",
      "controls": [
        "3"
      ]
    }
  ],
  "mitre_attack": [
    {
      "tactic": "Discovery",
      "technique": "T1087"
    }
  ]
}