{
  "id": "aws-eks-control-plane-logging",
  "name": "EKS Control Plane Logging",
  "description": "EKS cluster should have control plane logging enabled",
  "effect": "forbid",
  "resource": "aws::eks::cluster",
  "conditions": [
    "logging.cluster_logging.enabled != true"
  ],
  "severity": "medium",
  "tags": ["cis-eks-2.1.1", "eks", "kubernetes", "logging", "audit"],
  "remediation": "Enable all control plane logging types: api, audit, authenticator, controllerManager, scheduler."
}
