{
  "id": "aws-rds-logging-enabled",
  "name": "RDS Logging Enabled",
  "description": "RDS instances should export logs to CloudWatch",
  "effect": "forbid",
  "resource": "aws::rds::db_instance",
  "conditions": [
    "enabled_cloudwatch_logs_exports == null"
  ],
  "severity": "medium",
  "tags": ["rds", "database", "logging", "audit"],
  "remediation": "Enable CloudWatch log exports for audit, error, and slow query logs."
}
