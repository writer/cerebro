{
  "id": "wiz-db-dormant-principal",
  "wiz_control_id": "wc-id-2693",
  "name": "Database server with sensitive data can be accessed by a principal that has been inactive in the last 90 days",
  "description": "A dormant principal still has access to a database containing sensitive data",
  "effect": "forbid",
  "resource": "database::instance|database::cluster",
  "conditions": [
    "has_sensitive_data == true",
    "iam_policy.principals ANY (last_activity_days_ago > 90)"
  ],
  "severity": "medium",
  "remediation": "Review and remove access for dormant principals. Implement regular database access reviews.",
  "tags": ["wiz", "database", "dormant", "sensitive-data"]
}
