{
  "id": "wiz-ssh-key-unused",
  "wiz_control_id": "wc-id-912",
  "name": "SSH key pair is not used by any VM",
  "description": "An SSH key pair exists but is not associated with any VM",
  "effect": "forbid",
  "resource": "compute::ssh_key",
  "conditions": [
    "associated_instances_count == 0"
  ],
  "severity": "low",
  "remediation": "Delete unused SSH key pairs. Maintain key hygiene for security.",
  "tags": ["wiz", "compute", "ssh", "hygiene"]
}
