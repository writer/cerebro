{
  "id": "gcp-compute-metadata-v1",
  "name": "GCP Instance Metadata v1 Enabled",
  "description": "GCP Compute instance does not block legacy metadata endpoint (v1), making it vulnerable to SSRF attacks",
  "effect": "forbid",
  "principal": "*",
  "action": "compute.instances.create",
  "resource": "gcp::compute::instance",
  "conditions": [
    "metadata.enable-legacy-endpoints != 'false'"
  ],
  "severity": "high",
  "tags": ["cis-gcp-4.4", "ssrf", "metadata", "compute"],
  "remediation": "Set metadata key 'enable-legacy-endpoints' to 'false' to block the v1 metadata endpoint."
}
