name: Scheduled Docker Compose Smoke

on:
  schedule:
    # Run daily at 06:00 UTC (catch overnight dependency issues)
    - cron: '0 6 * * *'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  DOCKER_BUILDKIT: 1

jobs:
  smoke-compose:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4.1.0

      - name: Start services
        run: docker compose up -d --wait

      - name: Wait for Cerebro health
        run: |
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8080/healthz; then
              echo "Cerebro is healthy"
              exit 0
            fi
            echo "Waiting for Cerebro... ($i/30)"
            sleep 2
          done
          echo "Cerebro failed to become healthy"
          docker compose logs cerebro
          exit 1

      - name: Verify core endpoints
        run: |
          # Health endpoint should return 200
          curl -sf http://localhost:8080/healthz || (echo "Health check failed"; exit 1)
          api_key="$(docker compose exec -T cerebro printenv CEREBRO_API_KEYS)"
          api_key="${api_key%%:*}"
          test -n "$api_key" || (echo "Smoke API key unavailable"; exit 1)
          auth_header="Authorization: Bearer ${api_key}"
          curl -sf -H "$auth_header" http://localhost:8080/sources >/dev/null || (echo "Sources endpoint failed"; exit 1)
          curl -sf -H "$auth_header" 'http://localhost:8080/source-runtimes?tenant_id=local' >/dev/null || (echo "State-backed source runtimes endpoint failed"; exit 1)

          echo "Core endpoints verified"

      - name: Collect diagnostics on failure
        if: failure()
        run: |
          echo "=== Docker state ==="
          docker ps -a || true
          echo "=== NATS health ==="
          curl -sf http://localhost:8222/healthz || true
          echo "=== NATS logs ==="
          docker compose logs nats --tail 50 || true
          echo "=== Postgres logs ==="
          docker compose logs postgres --tail 50 || true
          echo "=== Neo4j logs ==="
          docker compose logs neo4j --tail 50 || true
          echo "=== Cerebro logs ==="
          docker compose logs cerebro --tail 100 || true

      - name: Cleanup
        if: always()
        run: docker compose down -v --remove-orphans
