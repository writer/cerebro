apiVersion: v1
kind: Secret
metadata:
  name: cerebro-secrets
  labels:
    app.kubernetes.io/name: cerebro
type: Opaque
stringData:
  # IMPORTANT: These are placeholders. In production, use external-secrets operator
  # or sealed-secrets to manage these values securely.
  # See k8s/base/external-secret.yaml for the recommended approach.
  SECRET_KEY: "REPLACE_WITH_SECURE_VALUE"
  DATABASE_URL: "REPLACE_WITH_SECURE_VALUE"
  REDIS_URL: "REPLACE_WITH_SECURE_VALUE"
