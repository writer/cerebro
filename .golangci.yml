version: "2"

run:
  timeout: 15m
  tests: false

linters:
  enable:
    - errcheck
    - staticcheck
    - govet
    - ineffassign
    - unused
    - bodyclose
    - noctx
    - errorlint
    - misspell

formatters:
  enable:
    - goimports
