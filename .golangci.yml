version: "2"

run:
  timeout: 10m
  tests: false

linters:
  enable:
    - errcheck
    - staticcheck
    - govet
    - ineffassign
    - unused
    - bodyclose
    - noctx
    - errorlint
    - misspell

formatters:
  enable:
    - goimports
